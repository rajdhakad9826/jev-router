{
  "name": "jev-router",
  "version": "1.0.0",
  "description": "Cost-aware LLM router that picks the cheapest model capable of handling a query, using TypeSafe's Jev for fast classification instead of an LLM call.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "llm",
    "llm-routing",
    "model-routing",
    "cost-optimization",
    "jev",
    "typesafe-ai"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rajdhakad9826/jev-router.git"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "0.6.0"
  }
}